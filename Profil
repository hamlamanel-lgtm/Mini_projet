<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8" />
    <title>Profile Page</title>
    <style>
        *{
            margin:0;
            padding:0;
            box-sizing:border-box;
            font-family: Arial, sans-serif;
        }

        body{
            background:#1f1f22;
            height:100vh;
            width:100vw;
            overflow:hidden;
            display:flex;
            color:white;
        }

        /* SAME SIDEBAR AS DASHBOARD */
        .sidebar{
            width:90px;
            background:#a476ff;
            border-top-right-radius:40px;
            border-bottom-right-radius:40px;
            display:flex;
            flex-direction:column;
            align-items:center;
            padding-top:25px;
            gap:20px;
        }

        .icon-box{
            width:55px;
            height:55px;
            background:#8b5cf6;
            border-radius:18px;
            display:flex;
            justify-content:center;
            align-items:center;
            cursor:pointer;
            transition:0.2s;
            font-size:22px;
        }

        .icon-box:hover{
            filter:brightness(1.1);
        }

        .bottom-icon{
            margin-top:auto;
            margin-bottom:20px;
        }

        /* MAIN PROFILE AREA */
        .profile-main{
            flex:1;
            padding:40px;
            display:flex;
            justify-content:center;
            align-items:flex-start;
        }

        .profile-card{
            width:100%;
            max-width:750px;
            background:#131315;
            border-radius:25px;
            padding:30px 40px;
        }

        .profile-header{
            text-align:center;
            margin-bottom:30px;
        }

        .profile-pic{
            width:70px;
            height:70px;
            background:#8b5cf6;
            border-radius:50%;
            display:flex;
            justify-content:center;
            align-items:center;
            margin:0 auto 10px;
            font-size:30px;
            font-weight:bold;
            color:black;
        }

        .profile-header h2{
            margin:5px 0 0;
        }

        /* FORM */
        .form-container{
            width:100%;
            margin:0 auto;
        }

        .row{
            display:flex;
            justify-content:space-between;
            gap:20px;
            margin-bottom:20px;
        }

        .field{
            flex:1;
            display:flex;
            flex-direction:column;
        }

        .field label{
            margin-bottom:5px;
            color:#d0d0d0;
            font-size:14px;
        }

        .field input,
        .field select{
            padding:10px 12px;
            background:#ffffff;
            border-radius:8px;
            border:1px solid #444;
            font-size:14px;
            color:#111;
            outline:none;
        }

        .field input[readonly],
        .field select[readonly]{
            background:#e5e5e5;
            color:#555;
            cursor:not-allowed;
        }

        .gender-box{
            display:flex;
            align-items:center;
            gap:15px;
            background:#ffffff;
            border-radius:8px;
            border:1px solid #444;
            padding:10px 12px;
            color:#111;
            font-size:14px;
        }

        .gender-box label{
            display:flex;
            align-items:center;
            gap:5px;
            color:#111;
            margin:0;
            font-size:14px;
        }

        /* DONE BUTTON */
        .done-btn{
            display:block;
            width:140px;
            margin:30px auto 0;
            padding:12px;
            text-align:center;
            background:#a476ff;
            border-radius:10px;
            cursor:pointer;
            font-weight:bold;
            border:none;
            font-size:14px;
            transition:0.2s;
            color:white;
        }

        .done-btn:hover{
            filter:brightness(1.1);
        }

        @media (max-width:800px){
            .profile-card{
                padding:20px;
            }
            .row{
                flex-direction:column;
            }
        }
    </style>
</head>
<body>

<!-- SIDEBAR (SAME AS DASHBOARD) -->
<div class="sidebar">
    <div class="icon-box">🔔</div>
    <div class="icon-box" onclick="window.location.href='dashboard.html'">🏠</div>
    <div class="icon-box">🎤</div>
    <div class="icon-box" onclick="window.location.href='Membership_fees.html'">💲</div>
    <div class="icon-box">👥</div>
    <div class="icon-box">📝</div>

    <div class="icon-box bottom-icon" onclick="window.location.href='Login.html'">↩</div>
</div>

<!-- PROFILE MAIN CONTENT -->
<div class="profile-main">
    <div class="profile-card">

        <div class="profile-header">
            <div class="profile-pic">M</div>
            <h2>Manel Hamla</h2>
            <!-- تم حذف hamla_manel -->
        </div>

        <div class="form-container">
            <!-- Row 1: Names -->
            <div class="row">
                <div class="field">
                    <label>Last Name</label>
                    <input type="text" value="Hamla">
                </div>

                <div class="field">
                    <label>First Name</label>
                    <input type="text" value="Manel">
                </div>
            </div>

            <!-- Row 2: Birthday + Gender -->
            <div class="row">
                <div class="field">
                    <label>Birthday</label>
                    <input type="date" value="2004-01-10">
                </div>

                <div class="field">
                    <label>Gender</label>
                    <div class="gender-box">
                        <label>
                            <input type="radio" name="g" checked> Female
                        </label>
                        <label>
                            <input type="radio" name="g"> Male
                        </label>
                    </div>
                </div>
            </div>

            <!-- Row 3: Email (readonly) + Member Code (readonly) -->
            <div class="row">
                <div class="field">
                    <label>Email</label>
                    <input type="text" value="hamlamanel@gmail.com" readonly>
                </div>

                <div class="field">
                    <label>Password</label>
                    <input type="text" value="1234" readonly>
                </div>
            </div>

            <!-- Row 4: Password + Role (readonly as Member) -->
            <div class="row">
                <div class="field">
                    <label>Role</label>
                    <input type="text" value="Member" readonly>
                </div>
            </div>

            <!-- DONE BUTTON -->
            <!-- غيّري 'dashboard.html' لاسم ملف الداشبورد عندك -->
            <button class="done-btn" onclick="window.location.href='dashboard.html'">
                Done
            </button>
        </div>

    </div>
</div>

</body>
</html>
